/* INOKaomoji — CRT 像素风颜文字渲染引擎
 * 颜文字先在低分辨率画布上用真实字体绘制，再像素化放大（imageSmoothing 关闭），
 * 每个逻辑像素格加 1px 细黑线网格，仿 CRT 显示器效果。
 * 数据驱动、零外部资源，API：init() / show(text) / showDefault() / current()
 */
(function () {
  'use strict';

  // ── 5×7 像素字模（每字符 7 行 × 5 bit，bit 从高位起 = 左列）────────
  var FONT = {
    ' ': [0,0,0,0,0,0,0],
    '!': [4,4,4,4,4,0,4],
    '"': [10,10,10,0,0,0,0],
    '#': [10,10,31,10,31,10,10],
    '$': [4,14,21,14,21,14,4],
    '%': [25,27,2,4,8,27,19],
    '&': [12,18,20,8,21,18,13],
    "'": [4,4,8,0,0,0,0],
    '(': [2,4,8,8,8,4,2],
    ')': [8,4,2,2,2,4,8],
    '*': [0,4,21,14,21,4,0],
    '+': [0,4,4,31,4,4,0],
    ',': [0,0,0,0,0,4,8],
    '-': [0,0,0,31,0,0,0],
    '.': [0,0,0,0,0,12,12],
    '/': [1,2,2,4,8,8,16],
    '0': [14,17,19,21,25,17,14],
    '1': [4,12,4,4,4,4,14],
    '2': [14,17,1,2,4,8,31],
    '3': [31,2,4,2,1,17,14],
    '4': [2,6,10,18,31,2,2],
    '5': [31,16,30,1,1,17,14],
    '6': [6,8,16,30,17,17,14],
    '7': [31,1,2,4,8,8,8],
    '8': [14,17,17,14,17,17,14],
    '9': [14,17,17,15,1,2,12],
    ':': [0,12,12,0,12,12,0],
    ';': [0,12,12,0,12,4,8],
    '<': [2,4,8,16,8,4,2],
    '=': [0,0,31,0,31,0,0],
    '>': [8,4,2,1,2,4,8],
    '?': [14,17,1,2,4,0,4],
    '@': [14,17,1,13,21,21,14],
    'A': [14,17,17,31,17,17,17],
    'B': [30,17,17,30,17,17,30],
    'C': [14,17,16,16,16,17,14],
    'D': [30,17,17,17,17,17,30],
    'E': [31,16,16,30,16,16,31],
    'F': [31,16,16,30,16,16,16],
    'G': [14,17,16,23,17,17,15],
    'H': [17,17,17,31,17,17,17],
    'I': [14,4,4,4,4,4,14],
    'J': [7,2,2,2,2,18,12],
    'K': [17,18,20,24,20,18,17],
    'L': [16,16,16,16,16,16,31],
    'M': [17,27,21,21,17,17,17],
    'N': [17,17,25,21,19,17,17],
    'O': [14,17,17,17,17,17,14],
    'P': [30,17,17,30,16,16,16],
    'Q': [14,17,17,17,21,18,13],
    'R': [30,17,17,30,20,18,17],
    'S': [15,16,16,14,1,1,30],
    'T': [31,4,4,4,4,4,4],
    'U': [17,17,17,17,17,17,14],
    'V': [17,17,17,17,17,10,4],
    'W': [17,17,17,21,21,27,17],
    'X': [17,17,10,4,10,17,17],
    'Y': [17,17,10,4,4,4,4],
    'Z': [31,1,2,4,8,16,31],
    '[': [14,8,8,8,8,8,14],
    '\\': [16,8,8,4,2,2,1],
    ']': [14,2,2,2,2,2,14],
    '^': [4,10,17,0,0,0,0],
    '_': [0,0,0,0,0,0,31],
    '`': [8,4,0,0,0,0,0],
    'a': [0,0,14,1,15,17,15],
    'b': [16,16,22,25,17,17,30],
    'c': [0,0,14,17,16,17,14],
    'd': [1,1,13,19,17,17,15],
    'e': [0,0,14,17,31,16,14],
    'f': [6,9,8,28,8,8,8],
    'g': [0,15,17,17,15,1,14],
    'h': [16,16,22,25,17,17,17],
    'i': [4,0,12,4,4,4,14],
    'j': [2,0,6,2,2,18,12],
    'k': [16,16,18,20,24,20,18],
    'l': [12,4,4,4,4,4,14],
    'm': [0,0,26,21,21,17,17],
    'n': [0,0,22,25,17,17,17],
    'o': [0,0,14,17,17,17,14],
    'p': [0,0,30,17,17,30,16],
    'q': [0,0,13,19,17,15,1],
    'r': [0,0,22,25,16,16,16],
    's': [0,0,15,16,14,1,30],
    't': [8,8,28,8,8,9,6],
    'u': [0,0,17,17,17,19,13],
    'v': [0,0,17,17,17,10,4],
    'w': [0,0,17,17,21,21,10],
    'x': [0,0,17,10,4,10,17],
    'y': [0,0,17,17,15,1,14],
    'z': [0,0,31,2,4,8,31],
    '{': [2,4,4,8,4,4,2],
    '|': [4,4,4,4,4,4,4],
    '}': [8,4,4,2,4,4,8],
    '~': [0,0,10,21,0,0,0],
    // ── 颜文字高频符号 ──
    'ω': [0,0,17,17,17,17,14],       // 猫嘴 ω
    '◕': [10,10,0,31,17,17,14],      // 大眼睛
    '‿': [0,0,0,17,10,4,0],          // 下弧
    '♥': [10,31,31,31,14,4,0],       // 实心爱心
    '♡': [0,10,21,21,14,4,0],        // 空心爱心
    '★': [4,4,14,31,14,4,4],         // 实心星
    '☆': [4,4,14,21,14,4,4],         // 空心星
    '✧': [0,4,10,17,10,4,0],         // 小闪星
    'ノ': [1,2,4,8,16,16,16],        // 片假名ノ（撇）
    'ﾉ': [1,2,4,8,16,16,16],         // 半角ﾉ
    'ヽ': [16,8,4,2,1,1,1],          // 片假名ヽ
    '゜': [0,0,12,18,18,12,0],       // 半浊点
    'ﾟ': [0,0,12,18,18,12,0],        // 半角ﾟ
    '｡': [0,0,0,0,0,12,12],          // 小句点
    '￣': [0,0,0,31,0,0,0],          // 上划线
    '▽': [0,0,17,17,10,4,0],        // 下三角
    '∀': [0,0,14,17,17,10,4],        // 倒 A
    'Д': [17,17,17,10,10,4,0],       // 西里尔 Д（眯眼）
    'ㅅ': [0,0,17,10,4,0,0],         // 韩文ㅅ
    'つ': [16,16,16,16,17,14,0],     // つ（侧脸）
    '・': [0,0,0,0,0,12,12],          // 中黑点
    'ー': [0,0,31,0,0,0,0],          // 长音符
    '〜': [0,0,10,21,0,0,0],         // 波浪号
    '─': [0,0,31,0,0,0,0],           // 横线
    '≧': [8,4,2,1,2,4,31],           // 眯眼 ≧（> 加底横）
    '≦': [2,4,8,16,8,4,31],          // 眯眼 ≦（< 加底横）
    '×': [0,17,10,4,10,17,0],        // 叉号
    '＾': [4,10,17,0,0,0,0],          // 全角 ^
    '＿': [0,0,0,0,0,0,31],          // 全角 _
  };

  var GLYPH_W = 5, GLYPH_H = 7, GLYPH_GAP = 1;  // （字模保留备用）
  var canvas = null, ctx = null;
  var currentText = 'OωO';

  // ── CRT 渲染参数：低分辨率 48×14，放大 5 倍 → 像素块够粗且字形仍可辨认 ──
  var LOW_W = 48, LOW_H = 14;
  var offCanvas = null, offCtx = null;

  // ── 判断文本是否全部有像素字模 → 走字模路径（像素块大而字形精准） ──
  function allInFont(t) {
    for (var k = 0; k < t.length; k++) {
      if (!FONT[t.charAt(k)]) return false;
    }
    return true;
  }

  // ── 字模路径：5×7 像素字体逐字符点阵绘制，每逻辑像素 = 一个大色块 ──
  function drawFont(t) {
    var W = canvas.width, H = canvas.height;
    var n = t.length;
    var cellsW = n * (GLYPH_W + GLYPH_GAP) - GLYPH_GAP; // 低清宽度（字符数×6-1）
    var s = Math.min(Math.floor(W / cellsW), Math.floor(H / GLYPH_H)); // 整数倍放大
    if (s < 1) s = 1;
    var totalW = cellsW * s, totalH = GLYPH_H * s;
    var x0 = Math.round((W - totalW) / 2), y0 = Math.round((H - totalH) / 2);
    ctx.clearRect(0, 0, W, H);
    ctx.fillStyle = '#f5c2e7';
    for (var i = 0; i < n; i++) {
      var glyph = FONT[t.charAt(i)];
      if (!glyph) continue;
      for (var row = 0; row < GLYPH_H; row++) {
        var bits = glyph[row];
        for (var col = 0; col < GLYPH_W; col++) {
          if (bits & (16 >> col)) {
            ctx.fillRect(x0 + (i * (GLYPH_W + GLYPH_GAP) + col) * s, y0 + row * s, s, s);
          }
        }
      }
    }
    // CRT 网格：每个大像素块边缘 1px 细黑线
    ctx.fillStyle = 'rgba(0,0,0,0.55)';
    for (var gx = x0 + s; gx < x0 + totalW; gx += s) ctx.fillRect(gx, y0, 1, totalH);
    for (var gy = y0 + s; gy < y0 + totalH; gy += s) ctx.fillRect(x0, gy, totalW, 1);
  }

  function draw(text) {
    if (!ctx) return;
    var t = String(text || 'OωO').slice(0, 14);

    // 0. 字模优先：全部字符有 5×7 字模 → 像素块大（8~10px）且字形精准
    if (allInFont(t)) { drawFont(t); return; }

    // 1. 低分辨率画布：真实字体直接绘制颜文字（完整覆盖复杂符号）
    if (!offCanvas) {
      offCanvas = document.createElement('canvas');
      offCanvas.width = LOW_W;
      offCanvas.height = LOW_H;
      offCtx = offCanvas.getContext('2d');
    }
    offCtx.clearRect(0, 0, LOW_W, LOW_H);
    offCtx.fillStyle = '#f5c2e7';
    offCtx.textBaseline = 'middle';
    var fs = 18;
    var setFont = function () {
      offCtx.font = 'bold ' + fs + 'px "Segoe UI Emoji","Segoe UI Symbol","Yu Gothic UI",sans-serif';
    };
    setFont();
    while (offCtx.measureText(t).width > LOW_W - 4 && fs > 5) { fs -= 1; setFont(); }
    var tw = offCtx.measureText(t).width;
    offCtx.fillText(t, Math.round((LOW_W - tw) / 2), LOW_H / 2 + 1);

    // 1.5 二值化：清除抗锯齿灰色杂点 → 像素块干净不脏（阈值以下全透明，以上全不透明）
    var img = offCtx.getImageData(0, 0, LOW_W, LOW_H);
    var px = img.data;
    for (var i = 0; i < px.length; i += 4) {
      if (px[i + 3] > 90) { px[i] = 245; px[i + 1] = 194; px[i + 2] = 231; px[i + 3] = 255; }
      else { px[i + 3] = 0; }
    }
    offCtx.putImageData(img, 0, 0);

    // 2. 像素化放大：关闭平滑 → 天然像素块
    var W = canvas.width, H = canvas.height;
    ctx.clearRect(0, 0, W, H);
    ctx.imageSmoothingEnabled = false;
    ctx.drawImage(offCanvas, 0, 0, W, H);

    // 3. CRT 像素网格：每格 1px 细黑线
    var sx = W / LOW_W, sy = H / LOW_H;
    ctx.fillStyle = 'rgba(0,0,0,0.55)';
    for (var gx = sx; gx < W - 1; gx += sx) ctx.fillRect(Math.round(gx), 0, 1, H);
    for (var gy = sy; gy < H - 1; gy += sy) ctx.fillRect(0, Math.round(gy), W, 1);
  }

  // ── 对外 API ───────────────────────────────────────────
  window.INOKaomoji = {
    init: function () {
      canvas = document.getElementById('kaomoji-canvas');
      if (!canvas) return;
      ctx = canvas.getContext('2d');
      draw(currentText);
    },
    show: function (text) {
      if (text) { currentText = String(text); draw(currentText); }
    },
    showDefault: function () {
      currentText = 'OωO';
      draw(currentText);
    },
    current: function () { return currentText; }
  };
})();
